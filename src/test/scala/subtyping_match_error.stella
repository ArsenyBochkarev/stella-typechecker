language core;

extend with #type-ascriptions, #references, #panic,
            #sum-types, #unit-type, #top-type, #lists, #variants,
            #ambiguous-type-as-bottom, #structural-subtyping;

fn main(x : Nat) -> Bot {
  return match panic! { _ => 0 }
}